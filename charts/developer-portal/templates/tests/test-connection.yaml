apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "developer-portal.fullname" . }}-test-connection"
  labels:
    {{- include "developer-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "developer-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
